#!/bin/sh
# After `git pull`: re-apply links (skips slow Homebrew and sudo-requiring shell steps)
command -v ansible-playbook >/dev/null 2>&1 || exit 0
cd "$(git rev-parse --show-toplevel)" || exit 0
ansible-playbook ansible/site.yml --limit "$(hostname -s)" --skip-tags homebrew,shell
